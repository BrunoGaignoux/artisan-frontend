'use strict';

const api = require('@generators/api');
const component = require('@generators/component');
const context = require('@generators/context');
const hoc = require('@generators/hoc');
const hook = require('@generators/hook');
const interfaces = require('@generators/interface');
const styles = require('@generators/styles');
const types = require('@generators/type');

module.exports = {
  api,
  component,
  context,
  hoc,
  hook,
  interfaces,
  styles,
  types,
};
