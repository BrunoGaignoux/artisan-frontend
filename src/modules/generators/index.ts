import api from '@generators/api';
import component from '@generators/component';
import context from '@generators/context';
import hoc from '@generators/hoc';
import hook from '@generators/hook';
import interfaces from '@generators/interface';
import styles from '@generators/styles';
import types from '@generators/type';

export default {
  api,
  component,
  context,
  hoc,
  hook,
  interfaces,
  styles,
  types,
};
